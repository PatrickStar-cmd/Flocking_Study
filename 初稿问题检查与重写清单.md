# 初稿问题检查与重写清单

> 整理日期：2026-09-07  
> 来源：研究方向与初稿重写总纲、初稿问题批注图片（两张）  
> 用途：逐项检查论文初稿的建模、理论、算法、仿真和表述。本文档是检查清单，不把待验证内容写成已证实结论。

## 一、最高优先级问题

### 1. CBF 安全约束的相对阶数错误

当前安全函数 (h_{ij}(q)) 对控制输入的相对阶为 2，而初稿直接使用一阶约束
[
dot h_{ij}+lambda h_{ij}ge 0,
]
其中不含控制输入 (u_i)，因此控制器无法直接通过该约束保证安全，后续“正不变性”结论不成立。

必须改为高阶/指数型 CBF，例如
[
ddot h_{ij}+k_1dot h_{ij}+k_0h_{ij}ge 0,
]
并明确：
- 安全函数、相对阶数和 Lie 导数；
- 使用真实位置还是估计位置；
- 估计误差和协方差如何进入鲁棒裕度；
- CBF-QP 是否可行；
- 多个邻居约束冲突时如何处理；
- 加速度、转向和输入饱和是否纳入约束。

如果暂时不能完成鲁棒 HOCBF/ECBF 推导，应删除“严格安全保证”等强表述，降级为仿真中的安全过滤器。

### 2. 定理 3 没有真正证明闭环稳定性

论文声称 Kalman、神经场、控制/软先验和估计构成闭环，但目前只是把“EKF 有界、控制器 ISS、CBF 可行”等性质直接作为假设或分别引用，不能推出整个闭环稳定。

需要补充：
- 统一的闭环状态、误差和扰动定义；
- 估计误差、神经场状态、运动状态之间的耦合关系；
- 小增益条件、复合 Lyapunov 函数或随机稳定性条件；
- 遮挡/丢包过程的假设；
- 有限 blackout 与长期不可观测时的结论边界；
- 证明中每个假设实际对应哪条算法或实验条件。

若无法给出完整证明，应将“定理”改成条件性命题、分析框架或可检验假设，不能把局部模块性质当作闭环稳定性证明。

### 3. 丢包模型没有真正进入 Kalman 滤波递推

初稿中的预测/更新公式未明确包含可见性变量 (delta_{ij}(k))。丢包时应只有预测步骤，不能继续执行测量更新：
[
hat x_{k|k-1},P_{k|k-1}
]
在 (delta=0) 时保留为后验状态；(delta=1) 时才进行观测更新。

必须说明：
- blackout 期间使用“带年龄衰减的预测轨迹”还是完全依靠神经场记忆；
- 协方差如何随连续丢包增长；
- 邻居控制输入是否可获得。若不可获得，不能直接使用 (u_j)，应使用已知模型、恒速/恒转率模型或过程噪声吸收；
- 遮挡恢复时如何处理过期观测；
- 公式中所有 (delta_{ij}(k)) 的含义和索引是否一致。

### 4. “软先验”实际上没有发挥约束作用

当 (kappa_i) 很低时，式(40)、(42) 仍把 (u_i^{NF}) 完整注入预测均值，仅增加协方差；式(43) 又使用全增益更新。此时所谓软先验并未真正减弱其对估计的影响。

需要明确一种可验证机制：
- 用 (kappa_i u_i^{NF}) 调节先验输入；
- 或使用概率融合/混合模型；
- 或让 (kappa_i) 调节过程噪声和观测噪声；
- 对 (kappa_iapprox0) 时的 (arg(c_i)) 不可靠问题设置降级策略；
- 说明 (GSigma G^	op) 如何传播；
- 比较固定权重、协方差加权和无软先验消融。

不能只把“不确定性”加到协方差，却继续把同一个不可靠预测均值完整用于估计。

### 5. 低通信条件下使用了不可获得的信息

初稿的相对观测、预测和 CBF 中出现 (u_j)、真实 (q_i,q_j)、真实相对位置或未定义的 (hat v_i) 等量，但低通信/局部观测条件下未必可获得。

必须逐式标注每个量的来源：
- 本机传感器；
- 邻居广播；
- 上一时刻缓存；
- 滤波器估计；
- 仿真中可见但现实不可见的真值。

特别检查：
- 式(14) 使用的 (u_j-u_i)；
- 式(42) 中 (hat u_j) 的定义；
- 式(43) 的势函数是否使用真实 (q_i-q_j)；
- CBF 是否使用估计位置和鲁棒裕度；
- 控制、速度和航向信息如何获得。

若实验依赖真实相对位置，应明确这是仿真真值基线，不能称为低通信可实现方法。

### 6. 离群点与混合离群噪声没有对应鲁棒算法

模型引入混合离群噪声，但后续仍使用标准 EKF/KF。标准滤波器不会自动抵抗 (D_{	ext{out}}) 型离群点。

至少需要一种明确方法：
- 创新门限检验；
- 鲁棒 M 估计；
- Student-t 滤波；
- 自适应观测噪声；
- 离群观测拒绝/降权。

同时给出阈值、误报/漏报处理和对照实验。否则应删去“离群鲁棒性”结论，只保留高斯噪声实验。

## 二、数学建模与符号检查

### 7. 动力学没有航向，无法支撑 ego/allo 比较

二阶点质量模型没有显式航向 (phi_i)，但论文比较 Egocentric 与 Allocentric 表征。应改为带航向的二维动力学，至少包含
[
dot q_i=v_i[cosphi_i,sinphi_i]^	op,qquad dotphi_i=omega_i.
]

同时定义机体坐标观测、世界坐标变换、ego 方位和 allo 方位。不能只在文字中宣称两种参考系不同。

### 8. KF、EKF 和 Kalman-Consensus Filter 术语混用

全文必须统一：
- 线性模型使用 KF；
- 非线性观测/动力学使用 EKF；
- 分布式一致性估计使用 Kalman-Consensus Filter；
- 明确每个滤波器的状态、输入、观测、噪声和更新式。

检查标题、摘要、算法框、公式编号和实验名称，避免同一方法在不同位置使用不同名称。

### 9. 公式、变量和索引未闭合

逐式检查：
- (h_{ij})、(dot h_{ij})、(ddot h_{ij}) 的定义；
- (q_i,p_i,v_i,phi_i,omega_i) 是否前后一致；
- (r_{ij}) 是真实值、观测值还是估计值；
- (hat u_j,hat v_i,hat q_j) 是否定义；
- (delta_{ij}(k))、(s_{ij}(k))、观测年龄 (a_{ij}) 的含义；
- 矩阵维度、转置和坐标系；
- 环形变量的 wrap、角度单位和周期边界；
- 协方差是否保持半正定。

### 10. 神经场、KF 与控制器的接口没有完全闭合

需要画出并用公式闭合：
观测 → KF → 协方差传播 → 神经场刺激 → 活动峰 → 方向/控制输入 → 动力学 → 下一次观测。

明确：
- 神经场输入是预测位置、预测方向还是活动峰；
- 自状态和相对状态是否重复计算；
- 神经场记忆在 blackout 期间如何更新；
- 协方差如何调节刺激权重；
- 神经场输出是否经过限幅、滤波和安全层。

## 三、遮挡、预测与信息质量

### 11. 遮挡模型要区分缺失、延迟和噪声

分别定义：
- 连续 blackout；
- 随机丢包；
- 观测延迟；
- 方位噪声；
- 距离噪声；
- 视场限制；
- 关键节点被遮挡；
- 遮挡恢复后的突变观测。

报告观测年龄、连续遮挡时长、有效邻居数和有效交互图最大连通分量。

### 12. 预测轨迹与神经场记忆的关系必须明确

丢包时应说明使用：
- 带协方差增长的 KF 预测；
- 上一次轨迹保持；
- 神经场活动峰记忆；
- 或两者融合。

不能一方面声称 blackout 时只有预测，另一方面又在邻居刺激中直接使用清零的真实邻居位置。需要说明“预测轨迹”与“删除邻居”的对照定义。

### 13. 协方差降权可能破坏连通性

权重
[
w_{ij}=exp(-alpha_	hetasigma^2_{	heta,ij})exp(-alpha_dsigma^2_{d,ij})exp(-eta_a a_{ij})
]
降低错误方向影响，但可能使有效交互图断裂。

必须单独报告：
- 最低有效权重；
- 最大连通分量比例；
- 每个个体的有效邻居数；
- 连通性保护策略；
- 过度降权与方向误差之间的权衡。

可信度权重不能与链路存在性保护混为一体。

### 14. 参考方向失效场景没有处理

Allocentric 依赖稳定世界参考方向。需要增加或明确排除：
- 航向估计误差；
- 外部参考漂移；
- GPS/磁罗盘不可用；
- 大航向噪声；
- 参考方向突然失效。

否则只能声称“在稳定外部参考存在时”的条件性优势。

## 四、控制、安全与实现

### 15. CBF-QP 的约束可行性和输入饱和未验证

除推导约束外，还要检查：
- 加速度、角速度或转向速率上限；
- QP 无解时的松弛变量；
- 松弛惩罚；
- 多约束冲突；
- 估计误差下的安全裕度；
- 最小距离是否用真值和估计值分别报告。

### 16. 安全约束与集群性能目标可能冲突

安全距离、凝聚、对齐和神经场方向控制可能互相冲突。实验应报告：
- 最小间距；
- 碰撞率；
- global/local order；
- 集群半径；
- 子群数量；
- QP 激活率和松弛率。

不要只报告方向指标而忽略安全约束导致的群体分裂。

### 17. “完全抵消遮挡”“适用于任意通信拒止”等表述需删除

长期不可观测时，任何预测都会积累误差。应改为：
- 在有限 blackout 时长和给定噪声条件下延缓失稳；
- 在指定连通性和参考方向条件下改善恢复；
- 对长期不可观测明确写出性能边界。

## 五、仿真与证据链

### 18. 当前仿真章节不能只写实验计划

必须补齐：
- 参数表；
- 初始条件；
- 随机种子；
- 遮挡序列；
- 集群规模；
- 视场和转向限制；
- Monte Carlo 重复次数；
- 均值、标准差或置信区间；
- 失稳判据；
- 结果图和统计检验。

### 19. 必须先复现理论基线

先复现 Salahshour–Couzin 的：
- ego/allo 对照；
- global order/local order；
- collective motion、aggregation、milling、fission-fusion 等状态；
- 规模或参数变化下的状态转变。

未复现基线时，不能把遮挡下差异归因于新方法。

### 20. 方法对照和消融要保持公平

至少比较：
1. 直接观测 + 基础 flocking；
2. 直接删除不可见邻居；
3. 上一时刻状态保持；
4. KF 预测；
5. KF + 固定神经场输入；
6. KF + 协方差加权；
7. KF + 协方差加权 + 方向记忆；
8. 自适应 ego/allo 表征。

所有方法使用相同初始状态、遮挡序列和随机种子。消融至少包括：去 KF、去协方差、去神经记忆、固定 ego、固定 allo、随机切换、去连通性保护。

### 21. 指标必须覆盖机制链

除 global order 外，应同时报告：
- local order；
- 神经活动峰漂移、分裂和同步误差；
- 方向估计误差；
- 失稳发生率和失稳时间；
- 平均群体半径；
- 子群数量；
- 最小间距；
- 最大连通分量比例；
- blackout 解除后的恢复时间；
- QP 激活/松弛率；
- 计算和通信开销。

### 22. “稳定区域扩大”必须由相图支持

若声称扩大稳定区域，至少需要扫描：
- 遮挡概率；
- 连续遮挡时长；
- 方位/距离噪声；
- 过程噪声；
- 有效邻居数；
- 视场角；
- 最大转向速度；
- 集群规模和初始密度。

不能用单一场景或单条轨迹支撑相图结论。

## 六、论文结构和表述检查

### 23. 引言主线应围绕一个机制问题

建议主线：
Allocentric flocking → 局部遮挡/观测不确定性 → 神经方向同步破坏 → global/local order 分离 → KF、协方差调制和表征切换的条件性补偿。

删除与主线无关或尚未实现的 GPS 拒止、目标跟踪、障碍物刺激等内容，除非它们有完整模型和实验。

### 24. 创新点必须与可复现实验证据一一对应

可以保留：
- 遮挡导致活动峰不同步和集群失稳的机制链；
- 协方差转化为神经刺激可信度；
- 固定/随机/自适应 ego-allo 表征的条件性比较。

不能直接宣称：
- Allocentric 在所有环境都优于 Egocentric；
- 神经场可以抵消长期遮挡；
- 普通 KF 自动识别离群点；
- 协方差降权一定保持连通性；
- 已经具有严格 ISS 或 CBF 安全保证。

### 25. 最终逐章检查顺序

1. 复现 Allocentric/Egocentric 基线；
2. 建立带航向的动力学和机体坐标观测；
3. 定义遮挡、观测年龄、协方差和有效交互图；
4. 统一 KF/EKF/分布式滤波术语；
5. 闭合神经场—滤波—控制接口；
6. 修正丢包递推和软先验；
7. 修正 HOCBF/鲁棒 CBF 或降级安全表述；
8. 增加离群点鲁棒算法或删除相关结论；
9. 完成遮挡相图、对照和消融；
10. 最后重写摘要、引言、理论结论和讨论边界。

## 七、交稿前一页式核对表

- [ ] 每个公式中的变量都有定义且坐标系一致。
- [ ] 每个估计量都说明来源，未使用不可获得的真实量。
- [ ] 丢包时只预测，不执行虚假的测量更新。
- [ ] 低可信神经先验确实降低了其均值影响。
- [ ] 标准 KF 没有被表述为自动抗离群点。
- [ ] CBF 相对阶数与控制输入匹配。
- [ ] CBF 使用估计量时包含误差裕度。
- [ ] 输入饱和、QP 不可行和约束冲突有处理。
- [ ] 协方差降权没有无意破坏连通性。
- [ ] ego/allo 的差异来自明确的航向和坐标变换。
- [ ] blackout、延迟、噪声和恢复场景定义清楚。
- [ ] 仿真有参数、随机种子、重复次数和统计区间。
- [ ] 所有强结论都有对应图表或证明。
- [ ] 待验证内容在全文中明确标注为假设、分析或未来工作。

