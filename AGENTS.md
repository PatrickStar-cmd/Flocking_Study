# Flocking_Study Agent 工作入口

本文件是仓库级 agent 协作入口。任何 agent 在开始工作前，必须先阅读本文件以及与任务相关目录中的 `AGENTS.md`、`AGENT_WORK_PROTOCOL.md` 和 `RESEARCH_ROADMAP.md`。

完整的三人协作、分支、实验、结果保护、论文、提交、审查和回退规则见：

- [协作约束框架_Flocking_Study.txt](协作约束框架_Flocking_Study.txt)

## 必须遵守的核心规则

1. 不直接修改 `main`；使用 `feature/...`、`exp/...`、`paper/...`、`docs/...` 或 `hotfix/...` 任务分支。
2. `allocentric-flocking-original/` 永远只读，禁止修改。
3. `effective-social-input/results/` 中已有实验结果默认只读；新实验必须使用新目录或新文件名，不得覆盖历史结果。
4. 开始工作前检查当前分支、`git status`、最近提交和其他成员的新改动。
5. 修改前说明目标、文件范围、参数、随机种子、测试计划和回退方式。
6. 修改后检查 `git diff`、运行相应 smoke test/回归测试，并独立复核至少一个关键统计量或计数。
7. 不使用 `git reset --hard`、强制推送或无确认的批量删除；公共历史使用 `git revert` 回退。
8. 不把软件测试、接口接通、单次轨迹或探索性结果写成科学收益、普适阈值、闭环稳定性或安全保证。
9. 只有方位观测时，不得声称恢复了二维邻居位置；普通 KF 不得表述为自动抗离群或安全保证。
10. 最终报告必须说明修改内容、未修改内容、测试结果、证据等级、限制和推荐的合并/回退方式。

## 任务开始提示

在执行任何修改前，先报告：

- 当前分支和目标分支；
- 将修改的文件；
- 明确不修改的受保护文件或结果；
- 参数、随机种子和输出目录；
- 测试与独立复核计划；
- 回退方式。

## 任务结束提示

完成后必须报告：

- 修改和新增的文件；
- 未修改的受保护内容；
- 分支、提交号和 diff 状态；
- 测试与统计复核结果；
- 这是软件修复、探索性、开发集、独立验证还是确认性证据；
- 尚未完成事项、风险和是否建议合并。
