# 终点敏感性实验状态（2026-09-07）

当前启动命令：`runPersistenceEndpointSensitivity(41:50)`，四条件共 40 次仿真。日志位于 `results/endpoint-sensitivity-20260907-172839.log`。运行器只在全部完成后写 CSV；未写出文件不表示未运行。

本轮是探索性旧种子重跑，不能称为独立验证。阈值来自种子 1–50 的运行均值分位数，应用于瞬时数据时统计含义发生变化。本轮结果仅说明在固定阈值下，时间聚合规则会改变判定。

当前实现的规则和限制详见 AGENTS.md。不要在运行过程中修改脚本，也不要启动重复任务。完成后检查 40 个唯一条件键、四组各十种子、全部布尔字段，再将原始汇总与历史同种子条件比较。保留失败日志。

后续必须完成：

1. 将逐步指标持久化，避免每更换分析规则都重跑动力学。
2. 分开比较块内原始指标均值判定与两层时间占比判定，不混用定义。
3. 不丢弃末尾窗口；报告 100 步=30 模型时间单位、300 步=90 单位、总评价时长=600 单位。
4. 以连续成功占比、失败段分布为主要描述结果，90%/95% 二元终点作为敏感性分析。
5. 不依据策略排序选取终点；正式规则须有任务性能或物理时间尺度依据。
6. 用合成序列验证全成功、全失败、短失败、末端失败与右删失。再进行未使用种子的确认性实验。

不得凭该十种子实验声称获得有效社会输入下限，也不能从基线通过率不高推出终点错误。
